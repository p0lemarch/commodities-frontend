const contractAddresses = require("./contractAddresses.json")
const { vaultabi, pricefeedabi, catabi } = require("./abis.json")

module.exports = {
    vaultabi,
    pricefeedabi,
    catabi,
    contractAddresses,
}
